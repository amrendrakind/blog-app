<div id="commentsSection">
                <% post.comments.each do |comment| %>
                  <%= render partial: "shared/single_comment", locals: { post: post, comment: comment } %>

                <% end %>
</div>  